# PostgreSQL bağlantı bilgileri (docker-compose.yml ile eşleşir)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=dijital_gardirop
DB_USER=postgres
DB_PASSWORD=postgres

# Backend'in kullanabileceği tek satırlık bağlantı adresi
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/dijital_gardirop
